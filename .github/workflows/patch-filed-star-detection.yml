name: Patch filed STAR detection

on:
  push:
    paths:
      - 'scripts/patch-filed-star-detection.cjs'
      - '.github/workflows/patch-filed-star-detection.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Apply patch
        run: node scripts/patch-filed-star-detection.cjs
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build
      - name: Check workspace function
        run: node --check functions/api/workspaces.js
      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Prioritize filed STAR entry fix for AUTO ETO"
          git push
