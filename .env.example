VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Cloudflare Pages Functions runtime variables/secrets.
# Configure these in Workers & Pages > atc-sequence > Settings > Variables and Secrets.
IVAO_CLIENT_ID=
IVAO_CLIENT_SECRET=
IVAO_API_KEY=
SESSION_SECRET=
IVAO_REDIRECT_URI=https://atc-sequence.pages.dev/api/auth/callback/ivao

# Staff Admin Console (server-side only; never expose as VITE_*).
SUPABASE_SERVICE_ROLE_KEY=
# Optional because production falls back to this project's URL.
SUPABASE_URL=https://jamwzmqcerkivkgpezfh.supabase.co
